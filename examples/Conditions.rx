# vim: set ft=rb:

class @
  def @main()
    def myvar : String = null
    def myarray = new Array@[String]()

    if(myvar isnt null and myvar.charAt(10) is "p")
      for(i in myarray.iterator())
        trace("Wouhou")
      end
    elsif(myvar isnt null and myvar.charAt(20) is "c")
      trace("no")
    else
      trace("coco")
    end

    if(myvar isnt null and
        myvar.charAt(0) is "x" and
        myvar.charAt(2) is "x")
      trace("ok")
    end
  end
end
