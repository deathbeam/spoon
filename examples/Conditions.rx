class self

def self.main()
  def myvar : String = null
  def myarray = Array(:String:).new()

  if myvar != null && myvar.charAt(10) == "p" then
    for i in myarray.iterator() loop
      trace("Wouhou")
    end
  elseif myvar != null && myvar.charAt(20) == "c" then
    trace("no")
  else
    trace("coco")
  end

  if myvar != null &&
      myvar.charAt(0) == "x" &&
      myvar.charAt(2) == "x" then
    trace("ok")
  end
end