#!/usr/bin/env coffee

import Sys

@message = "Hello"

a = 10
a = 15 + 8 * 6 / 7 + (8 - 7)

array = [ a, b, c ]

hash = [
  a: b,
  b: c,
  c: d
]

test = (a = 5) ->
  Sys.print "Jajaja"

[ foo, bar ] = baz
[ a, b ] = hello(world)
[ a: bar, b: baz ] = hello(world)

log-message = (message = "hello") ->
  message = "bb"
  a = 25
  b = 10
  Sys.print message
  return [ a, b ]

b = 15
test = TestClass!

log-message "Hello World"

if message
  Sys.print "[INFO] #{@message}"
else
  Sys.print "[INFO] Empty message"

for i in message
  Sys.print "#{i}"

while true do Sys.print "Hello"
