# vim: set ft=rb:

class @
  def @main()
    def myvar : String = empty
    def myarray = new Array@[String]()

    if(myvar != empty && myvar.charAt(10) == "p")
      for(i in myarray.iterator())
        trace("Wouhou")
      end
    elsif(myvar != empty && myvar.charAt(20) == "c")
      trace("no")
    else
      trace("coco")
    end

    if(myvar != empty &&
        myvar.charAt(0) == "x" &&
        myvar.charAt(2) == "x")
      trace("ok")
    end
  end
end
